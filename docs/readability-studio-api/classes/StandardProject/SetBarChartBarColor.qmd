## `SetBarChartBarColor`

If the bar charts' effect is to use a single color, sets the color to draw the bars with.

### Syntax {-}

``` {.lua}
SetBarChartBarColor(number red,
                    number green,
                    number blue)
```


### Parameters {-}

**Parameter** | **Description**
| :-- | :-- |
|`number` red | The red channel (should be between 0--255). | 
|`number` green | The green channel (should be between 0--255). | 
|`number` blue | The blue channel (should be between 0--255). |


{{< pagebreak >}}